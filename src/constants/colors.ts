export const firstTheme = {
  WHITE: "#FFF",
};

// const allThemes: {[key: string]: colorsTypes.TColors} = {
//   '1': firstTheme,
//   // Next themes with id's
// };

// const choseTheme = allThemes[themeId] ?? firstTheme;

export default firstTheme;
